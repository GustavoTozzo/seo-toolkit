# Scripts

Documentação completa de cada ferramenta (problema, como funciona, requisitos, exemplos)
está no [site do projeto](../README.md#ferramentas) ou em
[`src/content/tools.ts`](../src/content/tools.ts).

```bash
pip install -r requirements.txt
python <script>.py --help
```
